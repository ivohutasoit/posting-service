'use strict'

const Category = require('./category.model');
const Post = require('./post.model');
const Task = require('./task.model');

/**
 * @since 1.1.0
 */
module.exports = {
  Category,
  Post,
  Task
};